/submit/spend 
POST, returns a json 
input : 
    date (optional), title, price, user_token
output:
    {"status":"ok"}

/submit/income
POST, returns a json 
input :    
    data(optional), title, price, user_token
output:
    {"status":"ok"}
